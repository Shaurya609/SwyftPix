import React, { useEffect, useMemo, useState } from 'react';
import { ActivityIndicator, PixelRatio, ScrollView, StyleSheet, Text, TouchableOpacity, useWindowDimensions, View } from 'react-native';
import { Image } from 'expo-image';
import MaterialIcons from '@expo/vector-icons/MaterialIcons';
import { Gesture, GestureDetector, GestureHandlerRootView } from 'react-native-gesture-handler';
import Animated, { useAnimatedStyle, useSharedValue, withSpring } from 'react-native-reanimated';
import { MockMediaItem } from '../types/media';
import { formatFileSize, formatDate } from '../utils/formatters';
import { getPdfPageCount, readTextFile, renderPdfPage } from '../modules/swyftpix-media-delete';
import { readOfficeDocument, renderDocxHtml } from '../modules/swyftpix-media-delete/office-preview';
import SwyftPixOfficePreviewView from '../modules/swyftpix-media-delete/src/SwyftPixOfficePreviewView';

interface DocumentPreviewProps { item: MockMediaItem; thumbnail?: boolean; }
const AnimatedImage = Animated.createAnimatedComponent(Image);
type OfficeType = 'docx' | 'xlsx' | 'pptx';
function isPdfDocument(item: MockMediaItem) { return item.fileType === 'pdf' || item.fileName.toLowerCase().endsWith('.pdf') || item.mimeType?.toLowerCase() === 'application/pdf'; }
function getTextDocumentType(item: MockMediaItem) { const name = item.fileName.toLowerCase(); const mime = item.mimeType?.toLowerCase() ?? ''; if (['.txt', '.csv', '.log', '.json', '.xml'].some(ext => name.endsWith(ext)) || mime.startsWith('text/') || mime === 'application/json' || mime === 'application/xml') return 'text' as const; return null; }
function getOfficeType(item: MockMediaItem): OfficeType | null { const name = item.fileName.toLowerCase(); if (name.endsWith('.docx')) return 'docx'; if (name.endsWith('.xlsx')) return 'xlsx'; if (name.endsWith('.pptx')) return 'pptx'; return null; }
function getPdfRenderWidth(width: number, thumbnail: boolean) { if (thumbnail) return 900; return Math.min(Math.max(Math.round(width * PixelRatio.get() * 2.5), 1800), 2400); }
function officeLabel(type: OfficeType) { return type === 'docx' ? 'WORD' : type === 'xlsx' ? 'EXCEL' : 'POWERPOINT'; }
function officeIcon(type: OfficeType) { return type === 'xlsx' ? 'grid-on' : type === 'pptx' ? 'slideshow' : 'article'; }

export function DocumentPreview({ item, thumbnail = false }: DocumentPreviewProps) {
  const { width } = useWindowDimensions(); const isPdf = isPdfDocument(item); const textType = getTextDocumentType(item); const officeType = getOfficeType(item); const isText = !!textType; const isOffice = !!officeType;
  const [pageCount, setPageCount] = useState(0); const [pageIndex, setPageIndex] = useState(0); const [pageUri, setPageUri] = useState<string | null>(null); const [textContent, setTextContent] = useState<string | null>(null); const [officeContent, setOfficeContent] = useState<string | null>(null); const [docxHtml, setDocxHtml] = useState<string | null>(null); const [loading, setLoading] = useState(isPdf || isText || isOffice);
  const scale = useSharedValue(1); const savedScale = useSharedValue(1); const translateX = useSharedValue(0); const translateY = useSharedValue(0); const startX = useSharedValue(0); const startY = useSharedValue(0);
  const resetZoom = () => { scale.value = withSpring(1, { damping: 18, stiffness: 220 }); savedScale.value = 1; translateX.value = withSpring(0, { damping: 18, stiffness: 220 }); translateY.value = withSpring(0, { damping: 18, stiffness: 220 }); startX.value = 0; startY.value = 0; };

  useEffect(() => {
    let cancelled = false; setPageIndex(0); setPageCount(0); setPageUri(null); setTextContent(null); setOfficeContent(null); setDocxHtml(null); setLoading(isPdf || isText || isOffice); resetZoom();
    if (isOffice && officeType) {
      if (officeType === 'docx') { renderDocxHtml(item.uri).then(html => { if (cancelled) return; setDocxHtml(html); setLoading(false); }).catch(() => { if (cancelled) return; setDocxHtml(null); setLoading(false); }); }
      else { readOfficeDocument(item.uri, officeType, thumbnail ? 5000 : 300000).then(content => { if (cancelled) return; setOfficeContent(content); setLoading(false); }).catch(() => { if (cancelled) return; setOfficeContent(null); setLoading(false); }); }
      return () => { cancelled = true; };
    }
    if (isText) { readTextFile(item.uri, thumbnail ? 700 : 131072).then(text => { if (cancelled) return; setTextContent(text); setLoading(false); }).catch(() => { if (cancelled) return; setTextContent(null); setLoading(false); }); return () => { cancelled = true; }; }
    if (!isPdf) { setLoading(false); return () => { cancelled = true; }; }
    (async () => { const count = await getPdfPageCount(item.uri); if (cancelled) return; setPageCount(count); if (count > 0) { const rendered = await renderPdfPage(item.uri, 0, getPdfRenderWidth(width, thumbnail)); if (!cancelled) setPageUri(rendered); } if (!cancelled) setLoading(false); })();
    return () => { cancelled = true; };
  }, [item.id, item.uri, isPdf, isText, isOffice, officeType, thumbnail, width]);

  useEffect(() => { if (thumbnail || !isPdf || pageCount <= 0) return; let cancelled = false; resetZoom(); setPageUri(null); setLoading(true); renderPdfPage(item.uri, pageIndex, getPdfRenderWidth(width, false)).then(uri => { if (cancelled) return; setPageUri(uri); setLoading(false); }).catch(() => { if (cancelled) return; setPageUri(null); setLoading(false); }); return () => { cancelled = true; }; }, [item.id, item.uri, pageIndex, pageCount, thumbnail, isPdf, width]);
  const pinchGesture = useMemo(() => Gesture.Pinch().onUpdate(event => { scale.value = Math.min(4, Math.max(1, savedScale.value * event.scale)); }).onEnd(() => { if (scale.value <= 1.02) { scale.value = withSpring(1, { damping: 18, stiffness: 220 }); savedScale.value = 1; translateX.value = withSpring(0, { damping: 18, stiffness: 220 }); translateY.value = withSpring(0, { damping: 18, stiffness: 220 }); } else savedScale.value = scale.value; }), []);
  const panGesture = useMemo(() => Gesture.Pan().onStart(() => { startX.value = translateX.value; startY.value = translateY.value; }).onUpdate(event => { if (scale.value <= 1.01) return; const maxOffset = 220 * (scale.value - 1); translateX.value = Math.min(maxOffset, Math.max(-maxOffset, startX.value + event.translationX)); translateY.value = Math.min(maxOffset, Math.max(-maxOffset, startY.value + event.translationY)); }).onEnd(() => { if (scale.value <= 1.01) { translateX.value = withSpring(0, { damping: 18, stiffness: 220 }); translateY.value = withSpring(0, { damping: 18, stiffness: 220 }); } }), []);
  const gesture = useMemo(() => Gesture.Simultaneous(pinchGesture, panGesture), [pinchGesture, panGesture]); const pageAnimatedStyle = useAnimatedStyle(() => ({ transform: [{ translateX: translateX.value }, { translateY: translateY.value }, { scale: scale.value }] }));
  const goToPage = (nextIndex: number) => { if (nextIndex < 0 || nextIndex >= pageCount) return; setPageIndex(nextIndex); };

  if (thumbnail) {
    if (isPdf && pageUri) return <View style={styles.thumbnailContainer}><Image source={{ uri: pageUri }} style={styles.thumbnailPage} contentFit="contain" /></View>;
    if (isText && textContent) return <View style={styles.thumbnailContainer}><View style={styles.textThumbnail}><Text style={styles.textThumbnailContent} numberOfLines={7}>{textContent}</Text></View></View>;
    if (isOffice && officeType === 'docx' && docxHtml) return <View style={styles.nativeOfficeThumbnail}><SwyftPixOfficePreviewView html={docxHtml} style={styles.nativeOfficeFill} /></View>;
    if (isOffice && officeContent) return <View style={styles.thumbnailContainer}><View style={styles.officeThumbnail}><Text style={styles.officeThumbnailTitle}>{officeLabel(officeType!)}</Text><Text style={styles.officeThumbnailContent} numberOfLines={8}>{officeContent}</Text></View></View>;
    if ((isPdf || isText || isOffice) && loading) return <View style={styles.thumbnailContainer}><ActivityIndicator size="large" color="#0A7EA4" /></View>;
    return <View style={styles.thumbnailFallback}><View style={[styles.iconCircle, isPdf ? styles.pdfCircle : styles.documentCircle]}><MaterialIcons name={isOffice ? officeIcon(officeType!) as any : isPdf ? 'picture-as-pdf' : isText ? 'article' : 'description'} size={56} color="#FFFFFF" /></View><Text style={styles.thumbnailLabel}>{isOffice ? officeLabel(officeType!) : isPdf ? 'PDF' : isText ? 'TEXT' : 'DOCUMENT'}</Text></View>;
  }

  if (isPdf && pageUri) return <GestureHandlerRootView style={styles.root}><View style={styles.container}><View style={styles.viewerWrap}><GestureDetector gesture={gesture}><Animated.View style={styles.gestureArea}><AnimatedImage source={{ uri: pageUri }} style={[styles.page, pageAnimatedStyle]} contentFit="contain" /></Animated.View></GestureDetector>{loading ? <View style={styles.loadingOverlay}><ActivityIndicator size="large" color="#0A7EA4" /></View> : null}{pageCount > 1 ? <View style={styles.pageControls}><TouchableOpacity style={styles.pageButton} disabled={pageIndex === 0 || loading} onPress={() => goToPage(pageIndex - 1)}><MaterialIcons name="chevron-left" size={28} color={pageIndex === 0 || loading ? '#666666' : '#FFFFFF'} /></TouchableOpacity><Text style={styles.pageLabel}>Page {pageIndex + 1} of {pageCount}</Text><TouchableOpacity style={styles.pageButton} disabled={pageIndex === pageCount - 1 || loading} onPress={() => goToPage(pageIndex + 1)}><MaterialIcons name="chevron-right" size={28} color={pageIndex === pageCount - 1 || loading ? '#666666' : '#FFFFFF'} /></TouchableOpacity></View> : null}<View style={styles.zoomHint}><MaterialIcons name="zoom-in" size={16} color="#FFFFFF" /><Text style={styles.zoomHintText}>Pinch to zoom</Text></View></View></View></GestureHandlerRootView>;
  if (isOffice && officeType === 'docx' && docxHtml) return <View style={styles.nativeOfficeViewer}><SwyftPixOfficePreviewView html={docxHtml} style={styles.nativeOfficeFill} /></View>;
  if (isText && textContent) return <View style={styles.textViewer}><ScrollView contentContainerStyle={styles.textScrollContent} showsVerticalScrollIndicator><Text style={styles.textContent}>{textContent}</Text></ScrollView></View>;
  if (isOffice && officeContent) return <View style={styles.officeViewer}><View style={styles.officeHeader}><MaterialIcons name={officeIcon(officeType!) as any} size={24} color="#FFFFFF" /><Text style={styles.officeHeaderText}>{officeLabel(officeType!)} PREVIEW</Text></View><ScrollView contentContainerStyle={styles.officeScroll} showsVerticalScrollIndicator><Text style={styles.officeContent}>{officeContent}</Text></ScrollView></View>;
  return <View style={styles.genericViewer}>{loading ? <ActivityIndicator size="large" color="#0A7EA4" /> : <View style={[styles.iconCircle, isPdf ? styles.pdfCircle : styles.documentCircle]}><MaterialIcons name={isOffice ? officeIcon(officeType!) as any : isPdf ? 'picture-as-pdf' : isText ? 'article' : 'description'} size={68} color="#FFFFFF" /></View>}<Text style={styles.typeLabel}>{isOffice ? `${officeLabel(officeType!)} DOCUMENT` : isPdf ? 'PDF DOCUMENT' : isText ? 'TEXT DOCUMENT' : 'DOCUMENT'}</Text><Text style={styles.fileName} numberOfLines={3}>{item.fileName}</Text><View style={styles.metaRow}><Text style={styles.meta}>{formatFileSize(item.fileSize)}</Text><View style={styles.dot} /><Text style={styles.meta}>{formatDate(item.dateCreated)}</Text></View><Text style={styles.safeNote}>{isOffice ? (loading ? 'Preparing an in-app preview…' : 'This Office document could not be rendered on this device.') : isPdf ? (loading ? 'Preparing an in-app preview…' : 'PDF preview could not be rendered on this device.') : isText ? (loading ? 'Preparing an in-app preview…' : 'This text file could not be read on the device.') : 'In-app preview is currently available for supported document types.'}</Text></View>;
}

const styles = StyleSheet.create({
  root: { flex: 1, width: '100%' }, container: { flex: 1, width: '100%', backgroundColor: '#101010' }, viewerWrap: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 12, overflow: 'hidden' }, gestureArea: { width: '100%', height: '100%', justifyContent: 'center', alignItems: 'center' }, page: { width: '100%', height: '100%' }, loadingOverlay: { ...StyleSheet.absoluteFillObject, justifyContent: 'center', alignItems: 'center', backgroundColor: 'rgba(16,16,16,0.35)' }, pageControls: { position: 'absolute', bottom: 18, alignSelf: 'center', flexDirection: 'row', alignItems: 'center', gap: 12, backgroundColor: 'rgba(0,0,0,0.82)', paddingHorizontal: 8, paddingVertical: 7, borderRadius: 22 }, pageButton: { width: 40, height: 40, borderRadius: 20, justifyContent: 'center', alignItems: 'center' }, pageLabel: { color: '#FFFFFF', fontSize: 13, fontWeight: '700', minWidth: 90, textAlign: 'center' }, zoomHint: { position: 'absolute', top: 16, alignSelf: 'center', flexDirection: 'row', alignItems: 'center', gap: 6, backgroundColor: 'rgba(0,0,0,0.68)', paddingHorizontal: 12, paddingVertical: 7, borderRadius: 18 }, zoomHintText: { color: '#FFFFFF', fontSize: 12, fontWeight: '600' }, genericViewer: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 28, backgroundColor: '#101010' }, iconCircle: { width: 150, height: 150, borderRadius: 75, justifyContent: 'center', alignItems: 'center', marginBottom: 22 }, pdfCircle: { backgroundColor: '#FF3B30' }, documentCircle: { backgroundColor: '#3478F6' }, typeLabel: { color: '#FFFFFF', fontSize: 13, fontWeight: '800', letterSpacing: 1.8, marginBottom: 12 }, fileName: { color: '#FFFFFF', fontSize: 18, fontWeight: '700', textAlign: 'center', maxWidth: 340 }, metaRow: { flexDirection: 'row', alignItems: 'center', marginTop: 12 }, meta: { color: '#B0B0B0', fontSize: 13 }, dot: { width: 4, height: 4, borderRadius: 2, backgroundColor: '#777777', marginHorizontal: 9 }, safeNote: { color: '#777777', fontSize: 12, lineHeight: 18, textAlign: 'center', maxWidth: 330, marginTop: 20 }, thumbnailContainer: { flex: 1, width: '100%', height: '100%', backgroundColor: '#EDEDED', justifyContent: 'center', alignItems: 'center', overflow: 'hidden' }, thumbnailPage: { width: '100%', height: '100%' }, nativeOfficeThumbnail: { flex: 1, width: '100%', backgroundColor: '#EDEDED', overflow: 'hidden' }, nativeOfficeViewer: { flex: 1, width: '100%', height: '100%', backgroundColor: '#ECECEC', overflow: 'hidden' }, nativeOfficeFill: { flex: 1, width: '100%', height: '100%' }, thumbnailFallback: { alignItems: 'center', justifyContent: 'center' }, thumbnailLabel: { color: '#333333', fontSize: 14, fontWeight: '800', letterSpacing: 2, marginTop: 4 }, textThumbnail: { width: '92%', height: '88%', backgroundColor: '#FFFFFF', padding: 10, borderRadius: 4, overflow: 'hidden' }, textThumbnailContent: { color: '#222222', fontSize: 7, lineHeight: 10, fontFamily: 'monospace' }, officeThumbnail: { width: '92%', height: '88%', backgroundColor: '#FFFFFF', padding: 10, borderRadius: 4, overflow: 'hidden' }, officeThumbnailTitle: { color: '#2457A6', fontSize: 10, fontWeight: '900', marginBottom: 7 }, officeThumbnailContent: { color: '#333333', fontSize: 7, lineHeight: 10, fontFamily: 'monospace' }, textViewer: { flex: 1, backgroundColor: '#101010' }, textScrollContent: { padding: 18, paddingBottom: 40 }, textContent: { color: '#F2F2F2', fontSize: 14, lineHeight: 21, fontFamily: 'monospace' }, officeViewer: { flex: 1, backgroundColor: '#101010' }, officeHeader: { flexDirection: 'row', alignItems: 'center', gap: 9, paddingHorizontal: 16, paddingVertical: 12, backgroundColor: '#1D1D1D' }, officeHeaderText: { color: '#FFFFFF', fontSize: 13, fontWeight: '800', letterSpacing: 1.2 }, officeScroll: { padding: 18, paddingBottom: 48 }, officeContent: { color: '#F2F2F2', fontSize: 14, lineHeight: 22, fontFamily: 'monospace' }
});